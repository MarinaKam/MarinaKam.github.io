$(function () {

	$('.carousel-stage').jcarousel({
        itemLoadCallback: getCurrImage()
    });

    function getCurrImage(carousel, state){
        // console.log(carousel);
        var currentImage = carousel-1;
    }

});

